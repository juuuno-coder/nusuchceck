<!DOCTYPE html>
<html lang="ko">
<head>
  <title>누수체크 전문가 - 파트너 전문가 플랫폼</title>
  <meta name="viewport" content="width=device-width,initial-scale=1">
  <meta charset="utf-8">
  <%= csrf_meta_tags %>
  <%= csp_meta_tag %>
  <link rel="stylesheet" as="style" crossorigin
        href="https://cdn.jsdelivr.net/gh/orioncactus/pretendard@v1.3.9/dist/web/variable/pretendardvariable-dynamic-subset.min.css" />
  <%= stylesheet_link_tag "tailwind", "data-turbo-track": "reload" %>
  <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
  <%= javascript_importmap_tags %>
</head>
<body class="bg-gray-50 min-h-screen flex flex-col font-sans">
  <!-- Loading Progress Bar -->
  <div data-controller="loading">
    <div data-loading-target="bar" class="loading-bar hidden"></div>
  </div>

  <!-- Expert Navigation -->
  <nav class="bg-emerald-700 text-white shadow-lg" aria-label="전문가 메뉴" data-controller="mobile-menu">
    <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
      <div class="flex justify-between h-16">
        <div class="flex items-center">
          <%= link_to expert_root_path, class: "flex items-center space-x-2" do %>
            <span class="text-2xl font-bold">누수체크</span>
            <span class="text-xs bg-emerald-500 px-2 py-0.5 rounded-full font-medium">전문가</span>
          <% end %>

          <% if user_signed_in? && current_user.master? %>
            <div class="hidden md:flex ml-4 space-x-1">
              <%= link_to "배정된 체크", masters_requests_path, class: "px-3 py-2 rounded-md text-sm font-medium hover:bg-emerald-600" %>
              <%= link_to "보험청구", masters_insurance_claims_path, class: "px-3 py-2 rounded-md text-sm font-medium hover:bg-emerald-600" %>
              <%= link_to "내 프로필", masters_profile_path, class: "px-3 py-2 rounded-md text-sm font-medium hover:bg-emerald-600" %>
            </div>
          <% end %>
        </div>

        <div class="flex items-center space-x-3">
          <% if user_signed_in? %>
            <%= render "shared/notifications_dropdown" %>
            <span class="hidden sm:inline text-sm"><%= current_user.name %> (전문가)</span>
            <%= button_to "로그아웃", destroy_user_session_path, method: :delete, class: "hidden md:inline-flex px-3 py-2 rounded-md text-sm font-medium bg-emerald-800 hover:bg-emerald-900" %>
          <% else %>
            <%= link_to "로그인", new_user_session_path, class: "hidden sm:inline-flex px-3 py-2 rounded-md text-sm font-medium hover:bg-emerald-600" %>
            <%= link_to "전문가 가입", expert_sign_up_path, class: "hidden sm:inline-flex px-3 py-2 rounded-md text-sm font-medium bg-white text-emerald-700 hover:bg-gray-100" %>
          <% end %>

          <!-- 모바일 햄버거 버튼 -->
          <button type="button"
                  class="md:hidden inline-flex items-center justify-center p-2 rounded-md hover:bg-emerald-600 focus:outline-none focus:ring-2 focus:ring-white"
                  data-action="mobile-menu#toggle"
                  data-mobile-menu-target="button"
                  aria-expanded="false"
                  aria-label="메뉴 열기">
            <svg class="h-6 w-6" data-mobile-menu-target="iconOpen" fill="none" stroke="currentColor" viewBox="0 0 24 24" aria-hidden="true">
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M4 6h16M4 12h16M4 18h16"/>
            </svg>
            <svg class="h-6 w-6 hidden" data-mobile-menu-target="iconClose" fill="none" stroke="currentColor" viewBox="0 0 24 24" aria-hidden="true">
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M6 18L18 6M6 6l12 12"/>
            </svg>
          </button>
        </div>
      </div>
    </div>

    <!-- 모바일 메뉴 패널 -->
    <div class="md:hidden hidden" data-mobile-menu-target="panel">
      <div class="px-4 pt-2 pb-4 space-y-1 border-t border-emerald-600 mobile-menu-enter">
        <% if user_signed_in? && current_user.master? %>
          <div class="border-b border-emerald-600 pb-2 mb-2">
            <p class="px-3 py-1 text-emerald-200 text-sm"><%= current_user.name %> (전문가)</p>
          </div>
          <%= link_to "배정된 체크", masters_requests_path, class: "block px-3 py-2 rounded-md text-base font-medium hover:bg-emerald-600" %>
          <%= link_to "보험청구", masters_insurance_claims_path, class: "block px-3 py-2 rounded-md text-base font-medium hover:bg-emerald-600" %>
          <%= link_to "내 프로필", masters_profile_path, class: "block px-3 py-2 rounded-md text-base font-medium hover:bg-emerald-600" %>
          <div class="border-t border-emerald-600 my-2 pt-2">
            <%= button_to "로그아웃", destroy_user_session_path, method: :delete, class: "w-full text-left px-3 py-2 rounded-md text-base font-medium hover:bg-emerald-600" %>
          </div>
        <% else %>
          <%= link_to "로그인", new_user_session_path, class: "block px-3 py-2 rounded-md text-base font-medium hover:bg-emerald-600" %>
          <%= link_to "전문가 가입", expert_sign_up_path, class: "block px-3 py-2 rounded-md text-base font-medium bg-white text-emerald-700 hover:bg-gray-100 text-center mt-2" %>
        <% end %>
      </div>
    </div>
  </nav>

  <!-- Flash messages -->
  <%= render "shared/flash_messages" %>

  <!-- Main content -->
  <main class="flex-grow max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 py-8 w-full">
    <%= yield %>
  </main>

  <!-- Footer -->
  <footer class="bg-gray-800 text-gray-300 py-8 mt-auto">
    <div class="max-w-7xl mx-auto px-4 text-center">
      <p class="text-sm">&copy; <%= Date.current.year %> 누수체크 전문가. All rights reserved.</p>
      <p class="text-xs mt-2 text-gray-500">누수 전문 매칭 플랫폼 - 검증된 전문가의 파트너</p>
    </div>
  </footer>
</body>
</html>
