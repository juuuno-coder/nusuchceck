<!DOCTYPE html>
<html lang="ko">
<head>
  <title>누수체크 - 누수 전문 매칭 플랫폼</title>
  <meta name="viewport" content="width=device-width,initial-scale=1">
  <meta charset="utf-8">
  <%= csrf_meta_tags %>
  <%= csp_meta_tag %>
  <%= stylesheet_link_tag "tailwind", "inter-font", "data-turbo-track": "reload" %>
  <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
  <%= javascript_importmap_tags %>
</head>
<body class="bg-gray-50 min-h-screen flex flex-col">
  <!-- Navigation -->
  <nav class="bg-blue-700 text-white shadow-lg">
    <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
      <div class="flex justify-between h-16">
        <div class="flex items-center">
          <%= link_to root_path, class: "flex items-center space-x-2" do %>
            <span class="text-2xl font-bold">누수체크</span>
          <% end %>

          <%= link_to "AI 점검", new_leak_inspection_path, class: "ml-6 px-3 py-2 rounded-md text-sm font-medium hover:bg-blue-600" %>

          <% if user_signed_in? %>
            <div class="hidden md:flex ml-4 space-x-4">
              <% if current_user.customer? %>
                <%= link_to "내 신고", customers_requests_path, class: "px-3 py-2 rounded-md text-sm font-medium hover:bg-blue-600" %>
                <%= link_to "보험 신청", customers_insurance_claims_path, class: "px-3 py-2 rounded-md text-sm font-medium hover:bg-blue-600" %>
              <% elsif current_user.master? %>
                <%= link_to "배정된 신고", masters_requests_path, class: "px-3 py-2 rounded-md text-sm font-medium hover:bg-blue-600" %>
                <%= link_to "내 프로필", masters_profile_path, class: "px-3 py-2 rounded-md text-sm font-medium hover:bg-blue-600" %>
              <% elsif current_user.admin? %>
                <%= link_to "대시보드", admin_root_path, class: "px-3 py-2 rounded-md text-sm font-medium hover:bg-blue-600" %>
                <%= link_to "신고관리", admin_requests_path, class: "px-3 py-2 rounded-md text-sm font-medium hover:bg-blue-600" %>
                <%= link_to "마스터관리", admin_masters_path, class: "px-3 py-2 rounded-md text-sm font-medium hover:bg-blue-600" %>
                <%= link_to "에스크로", admin_escrow_transactions_path, class: "px-3 py-2 rounded-md text-sm font-medium hover:bg-blue-600" %>
                <%= link_to "보험관리", admin_insurance_claims_path, class: "px-3 py-2 rounded-md text-sm font-medium hover:bg-blue-600" %>
              <% end %>
            </div>
          <% end %>
        </div>

        <div class="flex items-center space-x-4">
          <% if user_signed_in? %>
            <span class="text-sm"><%= current_user.name %> (<%= current_user.display_role %>)</span>
            <%= button_to "로그아웃", destroy_user_session_path, method: :delete, class: "px-3 py-2 rounded-md text-sm font-medium bg-blue-800 hover:bg-blue-900" %>
          <% else %>
            <%= link_to "로그인", new_user_session_path, class: "px-3 py-2 rounded-md text-sm font-medium hover:bg-blue-600" %>
            <%= link_to "회원가입", new_user_registration_path, class: "px-3 py-2 rounded-md text-sm font-medium bg-white text-blue-700 hover:bg-gray-100" %>
          <% end %>
        </div>
      </div>
    </div>
  </nav>

  <!-- Flash messages -->
  <% if notice.present? %>
    <div class="max-w-7xl mx-auto px-4 mt-4">
      <div class="bg-green-100 border border-green-400 text-green-700 px-4 py-3 rounded relative" data-controller="dismissible" data-action="click->dismissible#dismiss">
        <%= notice %>
      </div>
    </div>
  <% end %>

  <% if alert.present? %>
    <div class="max-w-7xl mx-auto px-4 mt-4">
      <div class="bg-red-100 border border-red-400 text-red-700 px-4 py-3 rounded relative" data-controller="dismissible" data-action="click->dismissible#dismiss">
        <%= alert %>
      </div>
    </div>
  <% end %>

  <!-- Main content -->
  <main class="flex-grow max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 py-8 w-full">
    <%= yield %>
  </main>

  <!-- Footer -->
  <footer class="bg-gray-800 text-gray-300 py-8 mt-auto">
    <div class="max-w-7xl mx-auto px-4 text-center">
      <p class="text-sm">&copy; <%= Date.current.year %> 누수체크. All rights reserved.</p>
      <p class="text-xs mt-2 text-gray-500">누수 전문 매칭 플랫폼 - 투명한 가격, 검증된 전문가</p>
    </div>
  </footer>
</body>
</html>
